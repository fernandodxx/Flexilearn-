<%= turbo_frame_tag "modal" do %>
  <div class="fixed inset-0 bg-black bg-opacity-40 z-50 flex items-center justify-center">
    <div class="bg-white rounded-lg p-6 w-full max-w-xl shadow-lg relative">
      <h2 class="text-xl font-bold mb-4">Novo Módulo</h2>

      <%= render "form", lesson: @lesson %>

      <div class="mt-4 flex justify-end">
        <%= link_to "Cancelar", "#", class: "text-sm text-gray-500 hover:underline", data: { action: "click->modal#close" } %>
      </div>
    </div>
  </div>
<% end %>
