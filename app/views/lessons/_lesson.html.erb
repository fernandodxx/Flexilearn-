<div id="<%= dom_id(lesson) %>" class="border p-4 rounded-md bg-gray-50">
  <h3 class="text-xl font-bold mb-2"><%= lesson.title %></h3>
  <p class="text-gray-700 mb-3"><%= lesson.content %></p>

  <div class="text-sm text-gray-500">
    <strong>Trilha:</strong> <%= lesson.trail.title %>
  </div>
</div>

